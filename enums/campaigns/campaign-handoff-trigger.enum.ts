export enum CampaignHandoffTrigger {
  /** Created by a campaign handoff rule after the AI conversation completed */
  AUTOMATIC = 'automatic',
  /** Created by a user from the campaign target list */
  MANUAL = 'manual',
}
